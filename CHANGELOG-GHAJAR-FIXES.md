# تغییرات این نسخه

## ۱. OpenVPN (بود کرش می‌کرد / وصل نمی‌شد)
- کل لایه‌ی جاوای OpenVPN (۵۴ فایل) که کلاً از پروژه غایب بود، از ics-openvpn پورت شد.
  نسخه‌ی سخت‌شده (crash-safe) خود Ghajar از `OpenVPNService.java` حفظ شد.
- باگ دسترسی `StatusListener.init()` (package-private ولی از پکیج دیگه صدا زده می‌شد) رفع شد.
- فایل‌های native (`.so`) که فرستادی زیر `openvpn/src/main/jniLibs/<abi>/` قرار گرفتن.
- بیلد CMake/SWIG که به سورس ناموجود openvpn3 اشاره می‌کرد و هیچ‌وقت موفق نمی‌شد، با
  پیکربندی مبتنی بر باینری آماده جایگزین شد.

## ۲. فایل‌های کاملاً غایب از پروژه (یافته‌ی اصلی)
زیپ اولیه حدود ۳۵ فایل کد اصلی رو نداشت — از جمله خود `GozarVpnService.kt` (سرویس اصلی
VPN)، `ConfigStore`, `ProxyConfig`, `Strings` (سیستم زبان)، `IkeController`, `ConfigBuilder`
و باینری هسته‌ی Xray (`gozarcore.aar`, ۶۵ مگابایت). همه از GRoute (نسخه‌ی خواهر با همون
پکیج) بازیابی شدن؛ برندینگ باقی‌مونده (GRoute → Ghajar VPN) هم در تمام فایل‌ها اصلاح شد.

## ۳. باگ گیرکردن دکمه‌ی قطع/وصل
- مسیر OpenVPN قبلاً از `VpnCommandCoordinator` (mutex + watchdog) رد نمی‌شد؛ الان می‌شه.
- مسیر خطای تونل اصلی هیچ‌وقت به coordinator خبر «شکست خوردم» نمی‌داد؛ متد جدید
  `onTunnelFailed()` این حلقه رو می‌بنده تا broadcastهای بعدی به‌اشتباه فیلتر نشن.

## ۴. باگ آیکون نوار وظیفه (Quick Settings Tile)
- آیکون بعد از قطع/وصل روشن می‌موند چون `render()` بلافاصله بعد از یک درخواست async
  اجرا می‌شد و وضعیت هنوز به‌روز نشده بود؛ الان state «نیت کاربر» بلافاصله و به‌صورت
  optimistic نمایش داده می‌شه.
- لمس طولانی روی تایل، تنظیمات گوشی رو باز می‌کرد؛ با `setActivityLaunchForClick` درست شد.

## ۵. حذف مرورگر قاجار و ابزار کانفیگ
- ماژول `:browser` کامل حذف شد.
- برای صفحات غیرپرداختی (قوانین/راهنما/پنل کاربری) یک WebView سبک جایگزین ساخته شد
  (`GhajarStoreWebActivity`) — بدون آدرس‌بار، بدون نمایش لینک.
- هدر صفحه‌ی پرداخت به «درگاه پرداخت امن قاجار» تغییر کرد.
- `ConfigToolkitActivity` (خرابه بود) حذف شد؛ فایل‌های رمزگشا/مدل مشترک که «اتصال به
  کانفیگ» بهشون نیاز داره نگه داشته شدن.

## ۶. سیستم لاگ و اشکال‌زدایی پیشرفته
- `GhajarLog.kt`: بافر حافظه + فایل چرخشی روی دیسک، async و thread-safe.
- global crash handler: قبل از مرگ فرآیند، stack trace کامل + ۲۰۰ خط آخر لاگ سینک‌رون
  روی دیسک نوشته می‌شه.
- وصل شده به: تغییرات state اتصال، رفتار coordinator، لاگ داخلی OpenVPN، لاگ هسته‌ی
  Xray/Tor، کلید نوار وظیفه، خطاهای پرداخت.
- صفحه‌ی «لاگ و اشکال‌زدایی» (هم‌سبک بقیه‌ی اپ) با دانلود/اشتراک‌گذاری txt.

## ۷. لینک‌ها
- GitHub آپدیت‌چکر به `meysam82003/Ghajarvpn-` اصلاح شد.
- لینک تلگرام (`@Ghajarvpn` → `t.me/Ghajarvpn`) تأیید شد (از قبل درست بود).

## نکات مهم قبل از بیلد
- پروژه اینجا کامپایل/تست نشده (محیط من دسترسی به اینترنت/Android SDK نداره)؛ لطفاً
  اول توی Android Studio با Gradle sync کن و اگه خطای کامپایلی بود بفرست تا رفع کنم.
- اگه معماری گوشی‌های هدف با ۴ ABI فایل‌های `.so` ارسالی مطابقت نداره، ممکنه لازم باشه
  splits/abiFilters رو در `app/build.gradle.kts` بررسی کنی.
